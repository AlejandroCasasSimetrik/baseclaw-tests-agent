{
  "name": "baseclaw-tests-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Test suite for the BaseClaw agent architecture",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:watch": "vitest"
  },
  "dependencies": {
    "baseclaw-agent": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.0",
    "vitest": "^4.0.18"
  }
}
